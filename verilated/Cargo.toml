[package]
name = "verilated"
version = "0.0.0"
authors = ["Dan Glastonbury <dan.glastonbury@gmail.com>"]
publish = false

build = "build.rs"

[build-dependencies]
verilator = { path = "../verilator", default-features = false }
cc = "1.0"
